# 设计模式

可重用性、灵活性和可维护性。

常见的设计模式可以分为以下几类：

- 创建型模式：这些模式提供了创建对象的机制，增加了已有代码的灵活性和可复用性。包括单例模式、工厂方法模式、抽象工厂模式、建造者模式和原型模式。

- 结构型模式：这些模式关注类和对象的组合，形成更大的结构，同时保持它们的灵活性和高效性。包括适配器模式、装饰器模式、代理模式、外观模式、桥接模式、组合模式和享元模式。

- 行为型模式：这些模式专注于对象间的通信，即对象间如何相互作用以及怎样分配职责。包括策略模式、模板方法模式、观察者模式、迭代器模式、责任链模式、命令模式、备忘录模式、状态模式和访问者模式。

设计模式解决了以下问题：

代码复用：通过提供通用的解决方案，设计模式允许开发者重用代码，减少重复工作。
代码维护：通过将变化隔离在特定的类或模块中，设计模式使得代码更容易维护和更新。
设计灵活性：设计模式提供了一种灵活的设计方法，使得系统更容易适应变化。
通信和职责分配：设计模式帮助开发者明确对象间的通信方式和职责分配，从而提高代码的清晰度和可读性。
解耦：设计模式通过抽象和封装减少了模块间的依赖，提高了系统的解耦性。

## 设计原则

- 单一职责原则（Single Responsibility Principle, SRP）

一个类应该只有一个引起它变化的原因。这意味着一个类应该只负责一项任务或功能。

- 开闭原则（Open/Closed Principle, OCP）

类应该对扩展开放，对修改关闭。这意味着设计应该允许在不修改现有代码的情况下增加新功能。

- 里氏替换原则（Liskov Substitution Principle, LSP）

子类型必须能够替换掉它们的父类型。这意味着派生类应该能够无缝替换基类而不影响程序的行为。

- 接口隔离原则（Interface Segregation Principle, ISP）

不应该强迫客户依赖于它们不使用的方法。一个好的接口应该尽可能小且紧凑，只包含一个客户端需要的方法。

- 依赖倒置原则（Dependency Inversion Principle, DIP）

高层模块不应依赖于低层模块，两者都应该依赖于抽象；抽象不应依赖于细节，细节应依赖于抽象。这有助于减少模块间的耦合。

- 迪米特法则（Law of Demeter, LoD）

一个对象应该对其他对象有最少的了解。只与直接的朋友通信，不与“朋友的朋友”通信。

### 单例模式

```plantuml
@startuml

@enduml

```
